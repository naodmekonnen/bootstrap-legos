// check if distance to pump/fuelLeft <=mpg

const zeroFuel = (distanceToPump, mpg, fuelLeft) => {
//   let mpg = 25;
  if(distanceToPump/fuelLeft <= mpg){
    return  true;
  }
  else
    return false;
};
